<div>
<label for="title">Title</label>
<input type="text" name='title' value="<%=book.title%>" >
</div>
<div>
<label for="name">Author</label>
<select name='author'>
<% authors.forEach(author => {  %>
  <% if(author.id===book.author) { %>
    <option selected  value="<%= author.id %>"><%= author.name %></option>
  <% }else{%>
    <option  value="<%= author.id %>"/><%= author.name %></option>
  <% } %>
<%  }) %>
</select>
</div>
<div>
<label for="publish data">Publish Date</label>
<input type="date" name='publishDate' value="<%= book.publishDate==null? '':book.publishDate.toISOString().split("T")[0] %>" >
</div>
<div>
<label for="pageCount">Page Count</label>
<input type="number" name='pageCount' min='1' value="<%= book.pageCount %>" >
</div>
<div>
<label for="cover">Cover</label>
<input type="file" name='cover' class='filepond'>
</div>
<div>
<label for="description">Description</label>
<textarea type="text" name='cdescription' ><%= book.description%></textarea>
</div>


